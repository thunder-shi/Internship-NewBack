# Spring Boot Application Configuration
spring.application.name=newcms

# ==========================================
# Redis ??
# ==========================================
# ????????? exclude ??? Redis ?????
# ????????? Redis ??????????????? exclude?
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
spring.data.redis.repositories.enabled=false

# Redis ???? (???????????????????????)
spring.data.redis.host=47.96.172.199
spring.data.redis.port=6379
spring.data.redis.password=hfut1603
spring.data.redis.database=0
spring.data.redis.timeout=3000ms
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0

# ==========================================
# Server Configuration
# ==========================================
server.port=8111

# ==========================================
# DataSource Configuration
# ==========================================
spring.datasource.url=jdbc:mysql://47.96.172.199:3306/internship?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Hfut1603
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HikariCP ?????
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.pool-name=DatebookHikariCP
spring.datasource.hikari.connection-timeout=30000

# ==========================================
# JPA Configuration
# ==========================================
spring.jpa.database=mysql
spring.jpa.open-in-view=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Shanghai
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.use_sql_comments=false
# spring.jpa.hibernate.naming.physical-strategy=newcms.config.JpaNamingStrategy

# ==========================================
# Logging Configuration
# ==========================================
# SQL 日志：开发调试时改为 DEBUG，生产环境保持 WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.org.springframework.orm.jpa=WARN
logging.level.com.zaxxer.hikari=WARN
logging.level.newcms=INFO

# ==========================================
# Package Configuration
# ==========================================
entity.package=newcms.entity.db.
repository.package=newcms.repository.db.

# ==========================================
# Jackson Date Format
# ==========================================
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=Asia/Shanghai

# ==========================================
# MinIO OSS 配置
# ==========================================
minio.endpoint=http://47.96.172.199:9000
minio.accessKey=minioAdmin
minio.secretKey=minioAdmin
minio.bucketName=internship

# 文件上传大小限制
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=50MB

# ==========================================
# Coze 工作流（实习日志 AI 批改）
# ==========================================
coze.enabled=true
coze.api-base=https://api.coze.cn
coze.workflow-id=7649673784718295049
coze.pat=pat_RCyV3E3hTFDOqemidoWwP6iku4LlgKZAh315FH8Eo2z2CNz50zVz4PROpdiAfAvf
# 工作流开始节点入参名（本工作流为 file）
coze.file-url-param=file
# 文件入参：file-id=先上传到 Coze 再传 {"file_id":"..."}（推荐）；url=传 MinIO 直链
coze.file-input-mode=file-id
coze.file-url-expire-seconds=1800